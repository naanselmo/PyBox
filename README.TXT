Python 2.7 working as intended
Python 3.X untested and very likely incompatible

"python --version" outputs current python version


Client:
  "python box.py hostname port user directory"

Server:
  "python server.py port"
